apiVersion: v1
kind: Service
metadata:
  name: ollama-router
  namespace: ollama-router
  labels:
    app.kubernetes.io/name: ollama-router
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ollama-router
  ports:
    - name: http
      port: 8080
      targetPort: http
